# ============================================
# CONFIGURAÇÕES DE AMBIENTE
# ============================================
# Copie este arquivo para .env e preencha os valores
# NUNCA comite .env no repositório!
# ============================================

# Ambiente (development ou production)
NODE_ENV=development

# Porta (Render atribui automaticamente em produção)
PORT=3000

# ============================================
# FRONTEND (CORS)
# ============================================
# Desenvolvimento: http://localhost:5500
# Produção: https://seu-site.github.io/ecommerce-system
FRONTEND_URL=http://localhost:5500

# ============================================
# DATABASE (PostgreSQL)
# ============================================
# Desenvolvimento: localhost
# Produção: usar Render PostgreSQL ou Neon.tech
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecommerce_db
DB_USER=ecommerce_user
DB_PASSWORD=sua_senha_super_segura_aqui_min_32_chars

# ============================================
# SEGURANÇA (JWT)
# ============================================
# Gerar com: openssl rand -hex 32
# NUNCA use a mesma chave em dev e prod!
JWT_SECRET=cole_uma_chave_aleatoria_de_64_caracteres_aqui
JWT_EXPIRE=7d

# ============================================
# BCRYPT (Hash de senhas)
# ============================================
# Rounds de hash (10-12 é recomendado)
BCRYPT_ROUNDS=10

# ============================================
# RATE LIMITING (Proteção contra abuso)
# ============================================
# Janela de tempo em minutos
RATE_LIMIT_WINDOW=15
# Máximo de requisições por janela
RATE_LIMIT_MAX=100
